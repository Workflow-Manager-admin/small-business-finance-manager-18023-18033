# Environment variables for financial_frontend_app
# Copy this file to `.env` and adjust as appropriate for your deployment/environment.

# The full base URL where the FastAPI backend is running (protocol, host and port).
# Example for local cloud environment:
REACT_APP_BACKEND_URL=https://vscode-internal-112-beta.beta01.cloud.kavia.ai:3001
